function skillsMembers() {
    var members = [];
    var member = {
        name: 'jake',
        age: 25,
        skills: ['js', 'css', 'html']
    };

    members.push(member);

    member = {
        name: 'jane',
        age: 22,
        skills: ['js', 'css', 'html']
    };

    members.push(member);

    return members;
}